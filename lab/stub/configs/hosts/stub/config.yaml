# Generic Docker stub lab service.
operation_mode: stub
controller_url: http://unifi:8080/inform
profile: us8
mac: 02:15:6d:00:08:01
ip: 172.31.242.24
hostname: stub
uplink_speed: profile
no_discovery: true

# The generic lab service exposes the adoption SSH shim inside the isolated
# Compose network.
ssh_listen: 0.0.0.0:22
ssh_host_key_path: /var/lib/unifi-stubd/ssh_host_rsa_key
state_path: /var/lib/unifi-stubd/adoption.env
status_path: /var/lib/unifi-stubd/status.json
